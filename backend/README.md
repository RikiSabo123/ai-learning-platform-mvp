# Backend Service

This backend service uses Express, Sequelize, and PostgreSQL.

- Models are defined in `src/models`.
- API routes are in `src/routes`.
- Controllers and services handle request logic.
- The backend runs with Docker Compose using `docker-compose up --build`.
- If you run the backend locally without Docker, install dependencies with `npm install` and start with `npm start`.
